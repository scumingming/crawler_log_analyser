<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.isinonet.dao.CrawlerLogDao">
    <select id="getCrawlerLog" parameterType="java.lang.String" resultType="java.util.Map">
        SELECT 1 AS one,2 AS two;
    </select>
</mapper>